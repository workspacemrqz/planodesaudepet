Analise os seguintes problemas no sistema de plano de saude pet e implemente as correcoes necessarias:

PROBLEMA 1 - ERRO NO MODAL DO FAQ NO PAINEL ADMINISTRATIVO:
Na pagina /admin na aba FAQ, quando o usuario clica nos botoes Adicionar Pergunta ou Editar, aparece uma tela de erro com a mensagem Ocorreu um erro inesperado ao inves de abrir os modais corretos. Para corrigir este problema, verifique o arquivo client/src/components/admin/faq-tab.tsx e analise se os componentes Dialog do Radix UI estao sendo importados e renderizados corretamente. Examine se os event handlers handleAdd, handleEdit e handleDelete estao funcionando adequadamente. Verifique se as rotas da API /api/admin/faq, /api/admin/faq/:id estao retornando os dados corretamente no arquivo server/routes.ts. Para examinar problemas relacionados ao banco de dados, utilize a tool supabase no MCP para verificar se a tabela faq_items existe e tem a estrutura correta com as colunas id, question, answer, display_order, is_active e created_at. Certifique-se de que os estados isAddDialogOpen e isEditDialogOpen estao sendo gerenciados corretamente e que os componentes Dialog, DialogContent, DialogHeader e DialogTitle estao sendo importados de @/components/ui/dialog.

PROBLEMA 2 - REMOCAO DOS QUADRADOS COM NOME DO PLANO:
Nas paginas Inicio e /planos, os containers dos planos INFINITY COMFORT e PLATINUM exibem quadrados com background na cor #E5E7EB contendo o nome do plano que estao aparecendo atras dos icones SVG. Estes quadrados devem ser completamente removidos. Localize no codigo dos componentes client/src/components/sections/plans-section.tsx e client/src/components/ui/plan-card.tsx qualquer elemento div, span ou texto que esteja renderizando o nome do plano com background #E5E7EB ou classes Tailwind como bg-gray-200. Remova completamente esses elementos mantendo apenas os icones SVG dos planos. Verifique tambem se ha algum fallback de texto sendo renderizado quando a imagem nao carrega e remova-o.

PROBLEMA 3 - CORRECAO DO EMOJI NO PLANO BASIC:
Na pagina /planos, no container do plano BASIC, na secao Consultas, substitua o emoji atual do cabecalho por aquele usado na secao Procedimentos e Anestesia do mesmo plano. Localize o componente responsavel pela renderizacao dos detalhes do plano BASIC e identifique o emoji usado em Procedimentos e Anestesia para aplica-lo tambem em Consultas, garantindo consistencia visual.

PROBLEMA 4 - CORRECAO DAS IMAGENS NAO RENDERIZADAS:
As imagens nao estao sendo renderizadas ao iniciar o site. Verifique o sistema de imagens Base64 implementado no arquivo server/image-service-base64.ts e as rotas de imagem em server/routes.ts. Examine se as rotas /api/images/:type/:id estao funcionando corretamente e se o componente Base64ImageDisplay em client/src/components/Base64ImageDisplay.tsx esta processando e exibindo as imagens Base64 adequadamente. Para examinar o banco de dados, use a tool supabase no MCP para verificar se as colunas main_image, network_image e about_image na tabela site_settings e a coluna image_url na tabela network_units contem dados Base64 validos. Verifique tambem se o campo image na tabela plans esta preenchido corretamente. Certifique-se de que as imagens estaticas em client/public estao sendo servidas corretamente pelo Express em producao atraves das rotas configuradas em server/index.ts.

IMPLEMENTACAO:
Para cada problema, faca as seguintes acoes:
1. Identifique os arquivos especificos que precisam ser modificados
2. Analise o codigo atual para entender a causa raiz do problema
3. Implemente as correcoes mantendo a consistencia com o resto do sistema
4. Teste se os componentes continuam funcionando apos as mudancas
5. Garanta que nao haja regressoes em outras partes do sistema
6. Mantenha todos os estilos e funcionalidades existentes que nao estao relacionados aos problemas mencionados

Ao corrigir o problema do FAQ, certifique-se de que os modais abram corretamente, os formularios sejam exibidos e as operacoes CRUD funcionem adequadamente. Para os quadrados dos planos, remova apenas os elementos de texto com background sem afetar os icones ou outras partes do design. Para o emoji do plano BASIC, mantenha a formatacao e estrutura existentes alterando apenas o emoji especifico. Para as imagens, garanta que tanto imagens Base64 quanto arquivos estaticos sejam carregados corretamente em desenvolvimento e producao.